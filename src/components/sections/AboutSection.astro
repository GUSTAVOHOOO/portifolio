---
import { getCollection } from 'astro:content';
import HorizontalAbout from '../blocks/HorizontalAbout.tsx';

const team = await getCollection('team');
---

<section id="sobre" style="background: var(--color-bg); padding: 0;">
  <HorizontalAbout team={team} client:load />
</section>
