---
phase: 04-seo-performance-pre-launch-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/og-image.png
  - public/robots.txt
  - src/layouts/BaseLayout.astro
autonomous: false
requirements:
  - SEO-01
  - SEO-03

must_haves:
  truths:
    - "Sharing https://gmstudio.pages.dev in WhatsApp shows a 1200×630 preview image, the title in Portuguese, and the meta description"
    - "robots.txt at /robots.txt allows all bots and points to the sitemap"
    - "sitemap-index.xml is present in the build output at /sitemap-index.xml"
    - "PageSpeed Insights mobile score is documented — site is either passing (≥80) or a follow-up fix list exists"
    - "WhatsApp floating button dials the real number 5543996142514"
  artifacts:
    - path: "public/og-image.png"
      provides: "Open Graph image for social previews"
      dimensions: "1200×630px"
    - path: "public/robots.txt"
      provides: "Crawler directive file"
      contains: "Allow: /"
    - path: "src/layouts/BaseLayout.astro"
      provides: "OG + Twitter meta tags, correct WhatsApp number"
      exports: []
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "public/og-image.png"
      via: "og:image meta tag using Astro.site + '/og-image.png'"
      pattern: "og:image.*og-image\\.png"
    - from: "public/robots.txt"
      to: "sitemap-index.xml"
      via: "Sitemap: directive"
      pattern: "Sitemap:.*sitemap-index\\.xml"
---

<objective>
Pass all four launch gates for GMStudio: social preview correctness, WhatsApp phone number accuracy, crawler accessibility, and documented performance standing.

Purpose: The site is functionally complete after Phase 3. This plan makes it launch-ready by wiring OG metadata, fixing the WhatsApp placeholder, providing robots.txt, and recording the PageSpeed baseline so there are no surprises on launch day.

Output:
- `public/og-image.png` — 1200×630 static OG image with GMStudio branding
- `public/robots.txt` — allows all crawlers, points to sitemap
- `src/layouts/BaseLayout.astro` — OG + Twitter meta tags added, WhatsApp number corrected
- Human-verified PageSpeed Insights mobile score recorded
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-seo-performance-pre-launch-hardening/04-CONTEXT.md
@src/layouts/BaseLayout.astro
@astro.config.mjs

<interfaces>
<!-- Existing BaseLayout.astro — executor modifies this file directly. -->

From src/layouts/BaseLayout.astro (lines 1–13 — frontmatter):
```astro
const { title = 'GMStudio', description = 'GMStudio — Presença digital para empresas e empreendedores.' } = Astro.props;
const whatsappPhone = '5511999999999';   // ← replace with 5543996142514
const whatsappMessage = encodeURIComponent('Olá, vim pelo site da GMStudio');
```

From astro.config.mjs:
```js
site: 'https://gmstudio.pages.dev'   // Astro.site resolves to this value
```

Available logo files in src/assets/logo/:
- gmstudio-logo.svg       (full horizontal lockup, use this for OG image)
- gmstudio-logo-mono.svg  (white/mono variant)
- gmstudio-icon.svg       (square icon only)
- gmstudio-icon-mono.svg  (square icon mono)

Brand tokens (from CONTEXT.md):
- Background: #0A0A0F
- Accent indigo: #6366F1
- OG title per D-OG-02: "GMStudio — Presença digital para empresas"
- OG description per D-OG-03: "Criamos lojas online, cardápios digitais, landing pages e sites institucionais para empreendedores brasileiros."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create public/og-image.png and public/robots.txt</name>
  <files>public/og-image.png, public/robots.txt, scripts/generate-og.mjs</files>
  <action>
    Create a Node.js script at `scripts/generate-og.mjs` that generates `public/og-image.png` using the `canvas` package (install as dev dependency: `npm install --save-dev canvas`). The image must be exactly 1200×630px.

    OG image design (Claude's discretion per CONTEXT.md — D-OG-01 + brand tokens):
    - Fill background: #0A0A0F
    - Draw a subtle horizontal gradient band across the vertical center using rgba(99,102,241,0.08) to transparent (adds depth without distraction)
    - Draw a 3px horizontal line at y=580 in #6366F1 (accent underline — brand accent element)
    - Render the GMStudio wordmark: load `src/assets/logo/gmstudio-logo-mono.svg`, convert with `canvg` OR draw the text manually as fallback — if SVG loading is too complex, instead draw:
      - "GM" in #6366F1, font: bold 72px sans-serif, starting at x=80, y=280
      - "Studio" in #FFFFFF, font: 72px sans-serif, continuing horizontally
    - Below the wordmark at y=340, render tagline text: "Presença digital para empresas e empreendedores" in #A1A1AA (neutral-400 equivalent), font: 28px sans-serif
    - Save as PNG to `public/og-image.png`

    If `canvas` installation fails (native bindings can fail on Windows), use the SVG-to-PNG fallback:
    - Create `public/og-image.svg` instead with the same design using inline SVG elements
    - Run `npx sharp-cli -i public/og-image.svg -o public/og-image.png` to rasterize
    - If sharp-cli also fails, produce a minimal valid 1200×630 PNG by writing raw PNG bytes using only Node built-ins — this is a last resort; a plain dark background with text is acceptable as long as the file is a valid PNG at the correct dimensions

    After generating the PNG, verify dimensions: `node -e "const {createReadStream}=require('fs');const s=createReadStream('public/og-image.png');s.once('data',b=>{console.log('width:',b.readUInt32BE(16),'height:',b.readUInt32BE(20));s.destroy()})"` — must print `width: 1200 height: 630`.

    Then create `public/robots.txt` with exactly this content (per D-SEO-02):
    ```
    User-agent: *
    Allow: /
    Sitemap: https://gmstudio.pages.dev/sitemap-index.xml
    ```
  </action>
  <verify>
    <automated>node -e "const {createReadStream}=require('fs');const s=createReadStream('public/og-image.png');s.once('data',b=>{const w=b.readUInt32BE(16);const h=b.readUInt32BE(20);console.log('OG image:',w+'x'+h);if(w!==1200||h!==630)process.exit(1);s.destroy()})" && grep -q "Allow: /" public/robots.txt && grep -q "sitemap-index.xml" public/robots.txt && echo "robots.txt OK"</automated>
  </verify>
  <done>
    - `public/og-image.png` exists, is 1200×630px, has dark background and GMStudio branding
    - `public/robots.txt` exists with `Allow: /` and Sitemap directive pointing to `https://gmstudio.pages.dev/sitemap-index.xml`
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add OG + Twitter meta tags and fix WhatsApp number in BaseLayout.astro</name>
  <files>src/layouts/BaseLayout.astro</files>
  <action>
    Edit `src/layouts/BaseLayout.astro` with two changes:

    **Change 1 — WhatsApp phone number (per D-WA-01):**
    Replace `const whatsappPhone = '5511999999999';` with `const whatsappPhone = '5543996142514';`
    Remove the `// TODO: replace with real number` comment.

    **Change 2 — OG and Twitter meta tags (per D-OG-04):**
    In the frontmatter, derive the canonical URL and OG image URL from `Astro.site`:
    ```astro
    const siteUrl = Astro.site?.href.replace(/\/$/, '') ?? 'https://gmstudio.pages.dev';
    const ogImage = `${siteUrl}/og-image.png`;
    const ogTitle = 'GMStudio — Presença digital para empresas';
    const ogDescription = 'Criamos lojas online, cardápios digitais, landing pages e sites institucionais para empreendedores brasileiros.';
    ```

    Insert the following meta tags in `<head>` immediately after the existing `<meta name="description" .../>` tag:
    ```html
    <!-- Open Graph -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={siteUrl} />
    <meta property="og:title" content={ogTitle} />
    <meta property="og:description" content={ogDescription} />
    <meta property="og:image" content={ogImage} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:locale" content="pt_BR" />
    <meta property="og:site_name" content="GMStudio" />
    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={ogTitle} />
    <meta name="twitter:description" content={ogDescription} />
    <meta name="twitter:image" content={ogImage} />
    ```

    The existing `<title>{title}</title>` and `<meta name="description" content={description} />` lines remain unchanged — OG tags use their own fixed strings per D-OG-02 and D-OG-03, not the prop-driven values.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && grep -q "og:image" dist/index.html && grep -q "5543996142514" dist/index.html && echo "OG tags and WhatsApp number OK in build output"</automated>
  </verify>
  <done>
    - `dist/index.html` contains `og:image`, `og:title`, `og:locale`, `twitter:card` tags
    - `dist/index.html` contains `5543996142514` (real WhatsApp number)
    - `dist/sitemap-index.xml` present (confirms sitemap integration still works)
    - Build completes without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All SEO and pre-launch hardening assets:
    - `public/og-image.png` (1200×630, dark bg, GMStudio branding)
    - `public/robots.txt` (allows all crawlers, points to sitemap)
    - OG + Twitter meta tags in BaseLayout.astro
    - WhatsApp number corrected to 5543996142514
  </what-built>
  <how-to-verify>
    **Step 1 — Deploy the build:**
    Push to the `master` branch. Cloudflare Pages triggers automatically. Wait for the deployment at https://gmstudio.pages.dev.

    **Step 2 — Verify robots.txt:**
    Open https://gmstudio.pages.dev/robots.txt in a browser.
    Expected content:
    ```
    User-agent: *
    Allow: /
    Sitemap: https://gmstudio.pages.dev/sitemap-index.xml
    ```

    **Step 3 — Verify sitemap:**
    Open https://gmstudio.pages.dev/sitemap-index.xml.
    Expected: an XML document with at least one `<sitemap>` entry pointing to `sitemap-0.xml`.

    **Step 4 — Verify OG image:**
    Open https://gmstudio.pages.dev/og-image.png.
    Expected: a dark-background image, approximately 1200×630, with GMStudio branding visible.

    **Step 5 — Verify WhatsApp preview (OG tags):**
    Use https://www.opengraph.xyz — paste `https://gmstudio.pages.dev`.
    Expected:
    - Title: "GMStudio — Presença digital para empresas"
    - Description: "Criamos lojas online, cardápios digitais, landing pages e sites institucionais para empreendedores brasileiros."
    - Preview image: the dark-background GMStudio image (1200×630)

    **Step 6 — Run PageSpeed Insights:**
    Open https://pagespeed.web.dev — test `https://gmstudio.pages.dev` on **Mobile**.
    Record the scores:
    - Performance score (target: ≥80)
    - LCP (target: ≤2.5s)
    - CLS (target: ≤0.1)

    If all three pass → Phase 4 complete.
    If any fail → note the specific failing metric and the top opportunity listed by PageSpeed. A follow-up fix task will be created if needed.

    **Step 7 — Verify WhatsApp button phone number:**
    View page source (Ctrl+U) on https://gmstudio.pages.dev and search for `wa.me`.
    Expected: `https://wa.me/5543996142514`
  </how-to-verify>
  <resume-signal>
    Type "approved" if all checks pass.
    Or describe which checks failed and their values (e.g., "PageSpeed 74, LCP 3.1s") so a fix task can be created.
  </resume-signal>
</task>

</tasks>

<verification>
Build-time checks (run before checkpoint):
- `npm run build` exits 0
- `dist/index.html` contains all OG and Twitter meta property tags
- `dist/index.html` contains `5543996142514`
- `dist/sitemap-index.xml` exists in build output
- `public/robots.txt` contains `Allow: /` and `sitemap-index.xml` reference
- `public/og-image.png` is a valid PNG at 1200×630px

Production checks (human checkpoint):
- robots.txt accessible and correctly formatted at production URL
- sitemap-index.xml accessible at production URL
- OG tags render correct preview via opengraph.xyz debugger
- PageSpeed mobile score ≥80, LCP ≤2.5s, CLS ≤0.1
- WhatsApp button dials 5543996142514
</verification>

<success_criteria>
Phase 4 is complete when all of the following are TRUE:
1. Sharing https://gmstudio.pages.dev in WhatsApp (or via opengraph.xyz) shows a 1200×630 preview image, "GMStudio — Presença digital para empresas" as title, and the Portuguese meta description
2. https://gmstudio.pages.dev/robots.txt returns `Allow: /` and points to sitemap-index.xml
3. https://gmstudio.pages.dev/sitemap-index.xml is accessible and contains at least one sitemap entry
4. PageSpeed Insights mobile score ≥80, LCP ≤2.5s, CLS ≤0.1 (or a documented follow-up fix list exists if any threshold is missed)
5. WhatsApp floating button links to wa.me/5543996142514
</success_criteria>

<output>
After completion, create `.planning/phases/04-seo-performance-pre-launch-hardening/04-01-SUMMARY.md` following the summary template.

If the checkpoint reveals PageSpeed failures, document the failing metrics and top PageSpeed opportunities in the SUMMARY so a gap-closure plan can be created.
</output>
