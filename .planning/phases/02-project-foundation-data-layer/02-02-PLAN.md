---
phase: 02-project-foundation-data-layer
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/content.config.ts
  - src/data/services.json
  - src/data/team.json
  - src/data/testimonials.json
  - src/content/projects/padaria-do-joao.md
  - src/content/projects/barbearia-style.md
  - src/assets/projects/placeholder-600x400.jpg
  - src/assets/team/placeholder-400x400.jpg
autonomous: true
requirements: [SEO-02]

must_haves:
  truths:
    - "npm run build validates all JSON data files against Zod schemas without errors"
    - "getCollection('services') returns 4 service entries with id, title, description, icon"
    - "getCollection('projects') returns project entries with title, category, thumbnail, liveUrl"
    - "getCollection('team') returns team entries with id, name, role"
    - "getCollection('testimonials') returns testimonial entries with id, author, text"
  artifacts:
    - path: "src/content.config.ts"
      provides: "All collection schemas with Zod validation"
      exports: ["collections"]
      contains: "defineCollection"
    - path: "src/data/services.json"
      provides: "Service entries as top-level array"
      contains: "landing-page"
    - path: "src/data/team.json"
      provides: "Team member entries"
      contains: "name"
    - path: "src/data/testimonials.json"
      provides: "Client testimonial entries"
      contains: "author"
    - path: "src/content/projects/padaria-do-joao.md"
      provides: "Sample project with frontmatter"
      contains: "category"
  key_links:
    - from: "src/content.config.ts"
      to: "src/data/services.json"
      via: "file() loader"
      pattern: 'file.*services.json'
    - from: "src/content.config.ts"
      to: "src/content/projects/"
      via: "glob() loader"
      pattern: 'glob.*projects'
---

<objective>
Define all content collection schemas (services, team, testimonials, projects) with Zod validation and populate data files so Phase 3 components can consume type-safe collections.

Purpose: Satisfies SEO-02 by establishing the Image component pattern with placeholder assets. Ensures all data passes build-time validation.
Output: content.config.ts with 4 collections, populated JSON/MD data files, placeholder images.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-project-foundation-data-layer/02-RESEARCH.md
@.planning/phases/02-project-foundation-data-layer/02-UI-SPEC.md
@.planning/phases/02-project-foundation-data-layer/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create content.config.ts with all collection schemas</name>
  <files>src/content.config.ts</files>
  <read_first>
    - .planning/phases/02-project-foundation-data-layer/02-RESEARCH.md (Pattern 2: Content Collections — exact schema definitions, file() and glob() loader usage)
    - .planning/REQUIREMENTS.md (SERV-01 service types, ABOUT-02 team fields, TESTI-01 testimonial fields, PORTF-01/02 project fields)
  </read_first>
  <action>
    Create `src/content.config.ts` with these exact collection definitions:

    ```typescript
    import { defineCollection, z } from 'astro:content';
    import { file, glob } from 'astro/loaders';

    const services = defineCollection({
      loader: file("src/data/services.json"),
      schema: z.object({
        id: z.string(),
        title: z.string(),
        description: z.string(),
        icon: z.string(),
      }),
    });

    const team = defineCollection({
      loader: file("src/data/team.json"),
      schema: z.object({
        id: z.string(),
        name: z.string(),
        role: z.string(),
        photo: z.string().optional(),
        bio: z.string().optional(),
      }),
    });

    const testimonials = defineCollection({
      loader: file("src/data/testimonials.json"),
      schema: z.object({
        id: z.string(),
        author: z.string(),
        role: z.string().optional(),
        company: z.string().optional(),
        photo: z.string().optional(),
        text: z.string(),
      }),
    });

    const projects = defineCollection({
      loader: glob({ pattern: "**/*.md", base: "./src/content/projects" }),
      schema: z.object({
        title: z.string(),
        category: z.enum(["loja", "cardapio", "landing-page", "institucional"]),
        thumbnail: z.string(),
        liveUrl: z.string().url(),
        featured: z.boolean().default(false),
      }),
    });

    export const collections = { services, team, testimonials, projects };
    ```

    IMPORTANT: Use `import { z } from 'astro:content'` (not standalone zod) to avoid version conflicts per RESEARCH.md anti-patterns.
    IMPORTANT: All JSON files MUST be top-level arrays (not objects with nested arrays) per RESEARCH.md Pitfall 2.
  </action>
  <verify>
    <automated>cd "C:/Users/Windows 11/Documents/gastar-tokens" && grep -c "defineCollection" src/content.config.ts && grep "export const collections" src/content.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/content.config.ts contains `import { defineCollection, z } from 'astro:content'`
    - src/content.config.ts contains `import { file, glob } from 'astro/loaders'`
    - src/content.config.ts contains `loader: file("src/data/services.json")`
    - src/content.config.ts contains `loader: file("src/data/team.json")`
    - src/content.config.ts contains `loader: file("src/data/testimonials.json")`
    - src/content.config.ts contains `loader: glob({ pattern: "**/*.md", base: "./src/content/projects" })`
    - src/content.config.ts contains `z.enum(["loja", "cardapio", "landing-page", "institucional"])`
    - src/content.config.ts contains `export const collections = { services, team, testimonials, projects }`
  </acceptance_criteria>
  <done>content.config.ts defines 4 collections (services, team, testimonials, projects) with Zod schemas using file() and glob() loaders.</done>
</task>

<task type="auto">
  <name>Task 2: Populate data files, project markdown, and placeholder images</name>
  <files>src/data/services.json, src/data/team.json, src/data/testimonials.json, src/content/projects/padaria-do-joao.md, src/content/projects/barbearia-style.md, src/assets/projects/placeholder-600x400.jpg, src/assets/team/placeholder-400x400.jpg</files>
  <read_first>
    - src/content.config.ts (schema definitions — data must match exactly)
    - .planning/phases/02-project-foundation-data-layer/02-RESEARCH.md (sample services.json structure, sample project .md frontmatter)
    - .planning/phases/02-project-foundation-data-layer/02-UI-SPEC.md (image contract: 600x400 for project thumbnails, 400x400 for team photos)
  </read_first>
  <action>
    1. Create `src/data/` directory if not exists.

    2. Create `src/data/services.json` as a top-level array with 4 entries matching the 4 service types from SERV-01:
    ```json
    [
      {
        "id": "landing-page",
        "title": "Landing Page",
        "description": "Página de conversão de alta performance para campanhas e lançamentos.",
        "icon": "rocket"
      },
      {
        "id": "loja",
        "title": "Loja Online",
        "description": "E-commerce completo com catálogo, carrinho e checkout integrado.",
        "icon": "shopping-bag"
      },
      {
        "id": "cardapio",
        "title": "Cardápio Digital",
        "description": "Menu digital interativo para restaurantes, bares e delivery.",
        "icon": "utensils"
      },
      {
        "id": "institucional",
        "title": "Site Institucional",
        "description": "Presença digital profissional para empresas e empreendedores.",
        "icon": "building"
      }
    ]
    ```

    3. Create `src/data/team.json` as a top-level array with 2 placeholder entries:
    ```json
    [
      {
        "id": "gustavo",
        "name": "Gustavo",
        "role": "Fundador & Desenvolvedor",
        "photo": "/src/assets/team/placeholder-400x400.jpg",
        "bio": "Criador da GMStudio, apaixonado por transformar ideias em presença digital."
      },
      {
        "id": "membro-2",
        "name": "Membro da Equipe",
        "role": "Designer",
        "photo": "/src/assets/team/placeholder-400x400.jpg",
        "bio": "Placeholder — substituir com dados reais."
      }
    ]
    ```

    4. Create `src/data/testimonials.json` as a top-level array with 2 placeholder entries:
    ```json
    [
      {
        "id": "cliente-1",
        "author": "João Silva",
        "role": "Proprietário",
        "company": "Padaria do João",
        "text": "A GMStudio transformou minha padaria com uma loja online incrível. As vendas online cresceram 40% no primeiro mês."
      },
      {
        "id": "cliente-2",
        "author": "Maria Santos",
        "role": "Gerente",
        "company": "Barbearia Style",
        "text": "O site ficou exatamente como eu imaginava. Profissional, moderno e meus clientes adoraram."
      }
    ]
    ```

    5. Create `src/content/projects/` directory.

    6. Create `src/content/projects/padaria-do-joao.md`:
    ```markdown
    ---
    title: "Padaria do João"
    category: "loja"
    thumbnail: "/src/assets/projects/placeholder-600x400.jpg"
    liveUrl: "https://example.com/padaria-joao"
    featured: true
    ---

    Loja online completa para a Padaria do João, com catálogo de produtos, carrinho de compras e integração com delivery.
    ```

    7. Create `src/content/projects/barbearia-style.md`:
    ```markdown
    ---
    title: "Barbearia Style"
    category: "institucional"
    thumbnail: "/src/assets/projects/placeholder-600x400.jpg"
    liveUrl: "https://example.com/barbearia-style"
    featured: false
    ---

    Site institucional moderno para a Barbearia Style, com galeria de cortes, agendamento online e localização.
    ```

    8. Create placeholder images. Use ImageMagick or a simple script to generate solid-color placeholder JPGs:
    - `src/assets/projects/placeholder-600x400.jpg` — 600x400 pixels, dark gray (#1A1A24) background
    - `src/assets/team/placeholder-400x400.jpg` — 400x400 pixels, dark gray (#1A1A24) background

    If ImageMagick is not available, create minimal valid JPEG files using Node.js with sharp (already installed as an Astro dependency):
    ```javascript
    const sharp = require('sharp');
    sharp({ create: { width: 600, height: 400, channels: 3, background: { r: 26, g: 26, b: 36 } } }).jpeg().toFile('src/assets/projects/placeholder-600x400.jpg');
    sharp({ create: { width: 400, height: 400, channels: 3, background: { r: 26, g: 26, b: 36 } } }).jpeg().toFile('src/assets/team/placeholder-400x400.jpg');
    ```

    9. Run `npx astro build` to verify all collections pass Zod validation.
  </action>
  <verify>
    <automated>cd "C:/Users/Windows 11/Documents/gastar-tokens" && npx astro build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/data/services.json starts with `[` (top-level array)
    - src/data/services.json contains `"id": "landing-page"`
    - src/data/services.json contains `"id": "cardapio"`
    - src/data/services.json contains `"id": "institucional"`
    - src/data/services.json contains `"id": "loja"`
    - src/data/team.json starts with `[` (top-level array)
    - src/data/team.json contains `"id": "gustavo"`
    - src/data/testimonials.json starts with `[` (top-level array)
    - src/data/testimonials.json contains `"author": "João Silva"`
    - src/content/projects/padaria-do-joao.md contains `category: "loja"`
    - src/content/projects/barbearia-style.md contains `category: "institucional"`
    - src/assets/projects/placeholder-600x400.jpg exists
    - src/assets/team/placeholder-400x400.jpg exists
    - `npx astro build` completes without Zod validation errors
  </acceptance_criteria>
  <done>All 4 data sources populated and validated: services.json (4 entries), team.json (2 entries), testimonials.json (2 entries), 2 project .md files. Placeholder images created at correct dimensions. Build passes.</done>
</task>

</tasks>

<verification>
1. `npx astro build` completes without errors — all collections validated
2. `node -e "const s=require('./src/data/services.json'); console.log(Array.isArray(s), s.length)"` → `true 4`
3. `grep "category:" src/content/projects/*.md` returns valid categories
4. `test -f src/assets/projects/placeholder-600x400.jpg && echo OK`
</verification>

<success_criteria>
- content.config.ts defines 4 collections with Zod schemas
- All JSON data files are top-level arrays (Pitfall 2 avoided)
- All data passes build-time Zod validation
- Placeholder images exist at correct dimensions for SEO-02 Image component testing
- `npm run build` succeeds with all collections
</success_criteria>

<output>
After completion, create `.planning/phases/02-project-foundation-data-layer/02-02-SUMMARY.md`
</output>
