---
phase: 02-project-foundation-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - astro.config.mjs
  - tsconfig.json
  - src/styles/global.css
  - src/layouts/BaseLayout.astro
  - src/pages/index.astro
  - public/favicon.svg
autonomous: true
requirements: [HERO-02]

must_haves:
  truths:
    - "Running npm run dev starts an Astro dev server without errors"
    - "The site renders at 360px width without horizontal scroll"
    - "Tailwind utility classes using brand tokens (bg-bg, text-accent, font-heading) compile correctly"
    - "Google Fonts (Space Grotesk and Inter) load in the browser"
  artifacts:
    - path: "package.json"
      provides: "Astro 6 project with all dependencies"
      contains: "astro"
    - path: "astro.config.mjs"
      provides: "Astro config with Tailwind vite plugin, React, sitemap"
      contains: "tailwindcss"
    - path: "src/styles/global.css"
      provides: "Tailwind import + token mapping via @theme"
      contains: "@import"
    - path: "src/layouts/BaseLayout.astro"
      provides: "HTML shell with viewport meta, font loading, global CSS"
      contains: "viewport"
    - path: "src/pages/index.astro"
      provides: "Single page entry using BaseLayout"
      contains: "BaseLayout"
  key_links:
    - from: "src/styles/global.css"
      to: "src/styles/tokens.css"
      via: "@import ./tokens.css"
      pattern: '@import.*tokens'
    - from: "src/layouts/BaseLayout.astro"
      to: "src/styles/global.css"
      via: "import '../styles/global.css'"
      pattern: "import.*global.css"
    - from: "src/pages/index.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "import BaseLayout"
      pattern: "import.*BaseLayout"
---

<objective>
Initialize the Astro 6 project in the repo root, wire Tailwind CSS v4 with Phase 1 brand tokens, and create the BaseLayout with responsive viewport and font loading.

Purpose: Establishes the deployable project skeleton that all Phase 3 UI components will build on. Satisfies HERO-02 (mobile-first responsive layout).
Output: A running Astro dev server with brand-integrated Tailwind, BaseLayout, and index page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-project-foundation-data-layer/02-RESEARCH.md
@.planning/phases/02-project-foundation-data-layer/02-UI-SPEC.md
@src/styles/tokens.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Astro project and install all dependencies</name>
  <files>package.json, astro.config.mjs, tsconfig.json, public/favicon.svg</files>
  <read_first>
    - src/styles/tokens.css (must survive initialization — verify after)
    - src/assets/logo/gmstudio-icon.svg (source for favicon copy)
    - .planning/phases/02-project-foundation-data-layer/02-RESEARCH.md (installation steps and astro.config.mjs pattern)
  </read_first>
  <action>
    1. Run `npm create astro@latest . -- --template minimal --typescript strict --install --no-git` from the repo root. Accept any prompts about existing files — tokens.css and assets MUST survive.

    2. Verify Phase 1 files still exist after init: `src/styles/tokens.css`, `src/assets/logo/gmstudio-logo.svg`. If any were deleted, restore from git.

    3. Run `npx astro add tailwind` to install `@tailwindcss/vite` (NOT the deprecated `@astrojs/tailwind`).

    4. Run `npx astro add react` to install `@astrojs/react`.

    5. Run `npx astro add sitemap` to install `@astrojs/sitemap`.

    6. Run `npm install astro-seo @astrojs/check`.

    7. Edit `astro.config.mjs` to match this exact config:
    ```javascript
    import { defineConfig } from 'astro/config';
    import tailwindcss from '@tailwindcss/vite';
    import react from '@astrojs/react';
    import sitemap from '@astrojs/sitemap';

    export default defineConfig({
      site: 'https://gmstudio.pages.dev',
      integrations: [react(), sitemap()],
      vite: {
        plugins: [tailwindcss()],
      },
    });
    ```

    8. Copy `src/assets/logo/gmstudio-icon.svg` to `public/favicon.svg`.

    Per HERO-02: the project must be mobile-first responsive. This is established by the BaseLayout in Task 2.
  </action>
  <verify>
    <automated>cd "C:/Users/Windows 11/Documents/gastar-tokens" && node -e "const p=require('./package.json'); console.log('astro:', !!p.dependencies?.astro || !!p.devDependencies?.astro); console.log('tailwind:', !!p.dependencies?.tailwindcss || !!p.devDependencies?.tailwindcss);" && test -f src/styles/tokens.css && echo "tokens.css: OK" && test -f public/favicon.svg && echo "favicon: OK"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "astro" in dependencies or devDependencies
    - package.json contains "tailwindcss" in dependencies or devDependencies
    - package.json contains "@astrojs/react" in dependencies
    - package.json contains "@astrojs/sitemap" in dependencies
    - package.json contains "astro-seo" in dependencies
    - astro.config.mjs contains `import tailwindcss from '@tailwindcss/vite'`
    - astro.config.mjs contains `site: 'https://gmstudio.pages.dev'`
    - astro.config.mjs contains `plugins: [tailwindcss()]`
    - src/styles/tokens.css still exists (not overwritten by init)
    - src/assets/logo/gmstudio-logo.svg still exists
    - public/favicon.svg exists
  </acceptance_criteria>
  <done>Astro 6 project initialized with Tailwind v4 (via @tailwindcss/vite), React, sitemap, astro-seo. Phase 1 files intact. Favicon in public/.</done>
</task>

<task type="auto">
  <name>Task 2: Create global.css with Tailwind + token mapping, BaseLayout, and index page</name>
  <files>src/styles/global.css, src/layouts/BaseLayout.astro, src/pages/index.astro</files>
  <read_first>
    - src/styles/tokens.css (all CSS custom property names to map into @theme)
    - .planning/phases/02-project-foundation-data-layer/02-UI-SPEC.md (exact @theme mapping, font loading, responsive contract, copywriting contract)
    - .planning/phases/02-project-foundation-data-layer/02-RESEARCH.md (Pattern 1: Tailwind v4 + tokens, Pattern 4: BaseLayout)
  </read_first>
  <action>
    1. Create `src/styles/global.css` with this exact content:
    ```css
    @import "tailwindcss";
    @import "./tokens.css";

    @theme {
      --color-bg:             var(--color-bg);
      --color-surface:        var(--color-surface);
      --color-surface-raised: var(--color-surface-raised);
      --color-accent:         var(--color-accent);
      --color-accent-hover:   var(--color-accent-hover);
      --color-accent-surface: var(--color-accent-surface);
      --color-text:           var(--color-text);
      --color-text-muted:     var(--color-text-muted);
      --color-text-subtle:    var(--color-text-subtle);
      --color-border:         var(--color-border);
      --color-border-accent:  var(--color-border-accent);
      --font-heading:         var(--font-heading);
      --font-body:            var(--font-body);
    }

    body {
      overflow-x: hidden;
    }
    ```

    2. Create `src/layouts/BaseLayout.astro` with this exact content:
    ```astro
    ---
    import '../styles/global.css';
    interface Props {
      title?: string;
      description?: string;
    }
    const { title = 'GMStudio', description = 'GMStudio — Presença digital para empresas e empreendedores.' } = Astro.props;
    ---
    <!doctype html>
    <html lang="pt-BR">
      <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link
          href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600;700&family=Inter:wght@400;500;600&display=swap"
          rel="stylesheet"
        />
        <title>{title}</title>
        <meta name="description" content={description} />
      </head>
      <body class="bg-bg text-text font-body">
        <slot />
      </body>
    </html>
    ```

    3. Create `src/pages/index.astro`:
    ```astro
    ---
    import BaseLayout from '../layouts/BaseLayout.astro';
    ---
    <BaseLayout>
      <main class="min-h-screen flex items-center justify-center px-4">
        <h1 class="font-heading text-3xl font-semibold text-text">
          GMStudio
        </h1>
      </main>
    </BaseLayout>
    ```

    Key details per UI-SPEC:
    - `<html lang="pt-BR">` is mandatory
    - `<meta name="viewport" content="width=device-width, initial-scale=1.0">` is mandatory for HERO-02
    - `overflow-x: hidden` on body is a safety net per responsive contract
    - Google Fonts loads weights 400, 500, 600, 700 for Space Grotesk and 400, 500, 600 for Inter
    - Default title is "GMStudio", default description is "GMStudio — Presença digital para empresas e empreendedores." per copywriting contract
  </action>
  <verify>
    <automated>cd "C:/Users/Windows 11/Documents/gastar-tokens" && npx astro build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/styles/global.css contains `@import "tailwindcss"`
    - src/styles/global.css contains `@import "./tokens.css"`
    - src/styles/global.css contains `@theme {`
    - src/styles/global.css contains `--color-accent:         var(--color-accent)`
    - src/styles/global.css contains `--font-heading:         var(--font-heading)`
    - src/layouts/BaseLayout.astro contains `lang="pt-BR"`
    - src/layouts/BaseLayout.astro contains `width=device-width, initial-scale=1.0`
    - src/layouts/BaseLayout.astro contains `fonts.googleapis.com`
    - src/layouts/BaseLayout.astro contains `Space+Grotesk:wght@400;500;600;700`
    - src/layouts/BaseLayout.astro contains `Inter:wght@400;500;600`
    - src/layouts/BaseLayout.astro contains `class="bg-bg text-text font-body"`
    - src/pages/index.astro contains `import BaseLayout`
    - `npx astro build` completes without errors
  </acceptance_criteria>
  <done>global.css wires Tailwind v4 to Phase 1 tokens via @theme. BaseLayout provides responsive HTML shell with Google Fonts. Index page renders with brand styling. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `grep -r "viewport" src/layouts/BaseLayout.astro` returns the viewport meta tag
3. `grep "@theme" src/styles/global.css` returns the theme block
4. `grep "import.*tokens" src/styles/global.css` returns the tokens import
</verification>

<success_criteria>
- Astro dev server starts with `npm run dev`
- `npm run build` produces `dist/` output
- BaseLayout includes viewport meta, Google Fonts, brand tokens via Tailwind
- Index page renders at 360px without horizontal scroll
- All Phase 1 assets (tokens.css, logos) intact
</success_criteria>

<output>
After completion, create `.planning/phases/02-project-foundation-data-layer/02-01-SUMMARY.md`
</output>
