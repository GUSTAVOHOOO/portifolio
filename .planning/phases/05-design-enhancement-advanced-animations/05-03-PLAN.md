# Plan 05-03: Section Scroll Reveals, Stats Counter & Micro-interactions

**Phase:** 5 — Design Enhancement & Advanced Animations
**Goal:** Add Motion `whileInView` staggered entrances to ServicesSection, a number-counting React island to AboutSection stats, and CSS pulse + Motion `whileHover` micro-interactions to the floating WhatsApp button.
**Depends on:** Plan 05-01 (old animation system removed, motion installed)

---

## Success Criteria

- [ ] Service cards enter the viewport one by one with an 80ms stagger spring animation (fade-up + spring easing)
- [ ] Process steps in ServicesSection also stagger in on scroll
- [ ] The three stats in AboutSection (20+, 15+, 2+) count up from 0 to their target value when scrolled into view
- [ ] The floating WhatsApp button has a visible CSS pulse/glow ring animation
- [ ] All Motion animations are disabled when `prefers-reduced-motion: reduce` is set (counters jump to final value instantly)
- [ ] `npm run build` completes with zero errors
- [ ] `client:visible` is used for below-fold islands (ServicesAnimations, StatsCounter)

---

## Tasks

### Task 1: Create ServicesAnimations React island

**File:** `src/components/ServicesAnimations.tsx` (new file)

This island wraps the services grid and process steps with Motion staggered entrances. It receives the service data and process steps as props from the Astro parent.

```tsx
import { useReducedMotion, LazyMotion, domAnimation } from "motion/react"
import * as m from "motion/react-m"

interface Service {
  id: string
  title: string
  description: string
  icon: string
}

interface ProcessStep {
  number: number
  title: string
  description: string
}

interface Props {
  services: Service[]
  processSteps: ProcessStep[]
}

const iconPaths: Record<string, string> = {
  rocket: 'M4.5 16.5c-1.5 1.26-2 5-2 5s3.74-.5 5-2c.71-.84.7-2.13-.09-2.91a2.18 2.18 0 0 0-2.91-.09zm8.5-10L21 3l-3.5 8.5L12 15l-3-3 3.5-8.5zM12 15l-3-3 5.5-1.5L12 15zm-3-3L6.5 14 3 17l4-2 2-3z M15 9a4 4 0 0 0-4 4',
  'shopping-bag': 'M6 2 3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4zM3 6h18M16 10a4 4 0 0 1-8 0',
  utensils: 'M3 2v7c0 1.1.9 2 2 2h4a2 2 0 0 0 2-2V2M7 2v20M21 15V2a5 5 0 0 0-5 5v6c0 1.1.9 2 2 2h3zm0 0v7',
  building: 'M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z M9 22V12h6v10',
}

const container = {
  hidden: {},
  show: {
    transition: { staggerChildren: 0.08 },
  },
}

const item = {
  hidden: { opacity: 0, y: 20 },
  show: {
    opacity: 1,
    y: 0,
    transition: { type: 'spring' as const, stiffness: 300, damping: 30 },
  },
}

export default function ServicesAnimations({ services, processSteps }: Props) {
  const prefersReduced = useReducedMotion()

  const containerProps = prefersReduced
    ? {}
    : { variants: container, initial: 'hidden', whileInView: 'show', viewport: { once: true, margin: '0px 0px -60px 0px' } }

  const itemProps = prefersReduced
    ? {}
    : { variants: item }

  return (
    <LazyMotion features={domAnimation}>
      {/* Services grid */}
      <m.div className="services-grid" {...containerProps}>
        {services.map((service) => (
          <m.div
            key={service.id}
            {...itemProps}
            className="glass-card"
            style={{ padding: 'var(--space-8)' }}
          >
            <svg
              width="32"
              height="32"
              viewBox="0 0 24 24"
              fill="none"
              stroke="var(--color-accent)"
              strokeWidth="2"
              strokeLinecap="round"
              strokeLinejoin="round"
              aria-hidden="true"
            >
              <path d={iconPaths[service.icon] ?? iconPaths['building']} />
            </svg>
            <h3 style={{
              fontFamily: 'var(--font-heading)',
              fontSize: 'var(--text-xl)',
              fontWeight: 700,
              color: 'var(--color-text)',
              marginTop: 'var(--space-4)',
              marginBottom: 0,
            }}>
              {service.title}
            </h3>
            <p style={{
              fontFamily: 'var(--font-body)',
              fontSize: 'var(--text-base)',
              color: 'var(--color-text-muted)',
              marginTop: 'var(--space-2)',
              marginBottom: 0,
            }}>
              {service.description}
            </p>
          </m.div>
        ))}
      </m.div>

      {/* Process steps */}
      <div style={{ marginTop: 'var(--space-16)' }}>
        <h2 className="section-heading" style={{
          fontFamily: 'var(--font-heading)',
          fontSize: 'var(--text-3xl)',
          fontWeight: 700,
          lineHeight: 'var(--leading-snug)',
          letterSpacing: 'var(--tracking-tight)',
          color: 'var(--color-text)',
          textAlign: 'center',
          marginBottom: 'var(--space-12)',
          marginTop: 0,
        }}>
          Como trabalhamos
        </h2>
        <m.div className="process-steps" {...containerProps}>
          {processSteps.map((step) => (
            <m.div key={step.number} className="process-step" {...itemProps}>
              <div style={{
                width: '32px', height: '32px', borderRadius: '9999px',
                background: 'var(--color-accent-surface)', color: 'var(--color-accent)',
                fontFamily: 'var(--font-heading)', fontWeight: 700,
                display: 'flex', alignItems: 'center', justifyContent: 'center',
                fontSize: 'var(--text-sm)', flexShrink: 0,
              }}>
                {step.number}
              </div>
              <h3 style={{
                fontFamily: 'var(--font-heading)', fontSize: 'var(--text-xl)',
                fontWeight: 700, lineHeight: 'var(--leading-snug)',
                color: 'var(--color-text)', marginTop: 'var(--space-4)', marginBottom: 0,
              }}>
                {step.title}
              </h3>
              <p style={{
                fontFamily: 'var(--font-body)', fontSize: 'var(--text-sm)',
                color: 'var(--color-text-muted)', marginTop: 'var(--space-2)', marginBottom: 0,
              }}>
                {step.description}
              </p>
            </m.div>
          ))}
        </m.div>
      </div>
    </LazyMotion>
  )
}
```

---

### Task 2: Update ServicesSection.astro to use ServicesAnimations island

**File:** `src/components/ServicesSection.astro`

The frontmatter currently imports `getCollection` and builds services + processSteps data. Keep that data fetching. Add the import for the new island:

```astro
---
import { getCollection } from 'astro:content';
import ServicesAnimations from './ServicesAnimations.tsx';

const services = await getCollection('services');
const serviceData = services.map(s => ({
  id: s.id,
  title: s.data.title,
  description: s.data.description,
  icon: s.data.icon,
}))

const processSteps = [
  { number: 1, title: 'Briefing', description: 'Entendemos seu negócio, público e objetivos para alinhar expectativas antes de começar.' },
  { number: 2, title: 'Desenvolvimento', description: 'Criamos o layout e desenvolvemos cada detalhe do site com atenção ao design e à performance.' },
  { number: 3, title: 'Entrega', description: 'Publicamos o site, entregamos os acessos e garantimos que tudo está funcionando perfeitamente.' },
]
---
```

Replace the services grid HTML block and the process steps block with the island. The section structure becomes:

```astro
<section id="servicos">
  <div style="max-width: 80rem; margin: 0 auto;">
    <h2 class="section-heading">O que fazemos</h2>
    <ServicesAnimations
      services={serviceData}
      processSteps={processSteps}
      client:visible
    />
  </div>
</section>
```

Keep all `<style>` rules in the file unchanged (`.section-heading`, `.services-grid`, `.process-steps`, `.process-step` classes are still used by the island via `className`).

---

### Task 3: Create StatsCounter React island

**File:** `src/components/StatsCounter.tsx` (new file)

```tsx
import { useEffect, useRef } from "react"
import { LazyMotion, domAnimation, useMotionValue, useTransform, useInView } from "motion/react"
import * as m from "motion/react-m"
import { animate } from "motion/react"

interface Stat {
  numericValue: number
  suffix: string
  label: string
}

interface Props {
  stats: Stat[]
}

function Counter({ numericValue, suffix, label }: Stat) {
  const count = useMotionValue(0)
  const rounded = useTransform(count, (v) => Math.round(v))
  const ref = useRef<HTMLDivElement>(null)
  const isInView = useInView(ref, { once: true })

  useEffect(() => {
    if (!isInView) return
    const prefersReduced =
      typeof window !== 'undefined' &&
      window.matchMedia('(prefers-reduced-motion: reduce)').matches

    if (prefersReduced) {
      count.set(numericValue)
    } else {
      animate(count, numericValue, { duration: 1.5, ease: 'easeOut' })
    }
  }, [isInView, count, numericValue])

  return (
    <div ref={ref} style={{ textAlign: 'center' }}>
      <div className="stat-value" style={{
        fontFamily: 'var(--font-heading)',
        fontSize: 'var(--text-5xl)',
        fontWeight: 700,
        color: 'var(--color-accent)',
        lineHeight: 1,
      }}>
        <m.span>{rounded}</m.span>{suffix}
      </div>
      <div style={{
        fontFamily: 'var(--font-body)',
        fontSize: 'var(--text-sm)',
        color: 'var(--color-text-muted)',
        textTransform: 'uppercase',
        letterSpacing: 'var(--tracking-wide)',
        marginTop: 'var(--space-1)',
      }}>
        {label}
      </div>
    </div>
  )
}

export default function StatsCounter({ stats }: Props) {
  return (
    <LazyMotion features={domAnimation}>
      <div className="stats-row" style={{
        display: 'flex',
        gap: 'var(--space-8)',
        justifyContent: 'center',
      }}>
        {stats.map((stat) => (
          <Counter key={stat.label} {...stat} />
        ))}
      </div>
    </LazyMotion>
  )
}
```

---

### Task 4: Update AboutSection.astro to use StatsCounter island

**File:** `src/components/AboutSection.astro`

In the frontmatter, add the import and transform the stats array into the format the island expects (separate `numericValue` and `suffix`):

```astro
---
import { getCollection } from 'astro:content';
import StatsCounter from './StatsCounter.tsx';

const team = await getCollection('team');

// Parse numeric value and suffix from stat strings like "20+", "2+"
const statsData = [
  { numericValue: 20, suffix: '+', label: 'Projetos' },
  { numericValue: 15, suffix: '+', label: 'Clientes' },
  { numericValue: 2,  suffix: '+', label: 'Anos' },
]

const values = [
  { name: 'Transparência', description: 'Comunicação clara em todas as etapas do projeto.' },
  { name: 'Qualidade', description: 'Cada detalhe importa. Entregamos menos para fazer melhor.' },
  { name: 'Resultado', description: 'O sucesso do seu negócio é a nossa métrica de sucesso.' },
  { name: 'Parceria', description: 'Não somos apenas fornecedores — somos parceiros do seu crescimento.' },
]
---
```

In the template, replace the `.stats-row` div block (the one with the static `{stat.value}` display) with:

```astro
<StatsCounter stats={statsData} client:visible />
```

Keep the rest of `AboutSection.astro` (values grid, team cards, section headings, `<style>` block) exactly as-is.

---

### Task 5: Add CSS pulse animation to WhatsApp floating button

**File:** `src/layouts/BaseLayout.astro`

In the `<style>` block that was added in Plan 05-01 (which contains `.whatsapp-float:hover`), add the pulse keyframe and apply it:

```css
<style>
  @keyframes whatsapp-pulse {
    0%, 100% { box-shadow: 0 0 0 0 rgba(37, 211, 102, 0.5); }
    50%       { box-shadow: 0 0 0 12px rgba(37, 211, 102, 0); }
  }

  .whatsapp-float {
    animation: whatsapp-pulse 2.5s ease-in-out infinite;
  }

  .whatsapp-float:hover {
    background: #128C7E !important;
    animation-play-state: paused;
  }

  @media (prefers-reduced-motion: reduce) {
    .whatsapp-float {
      animation: none;
    }
  }
</style>
```

This replaces the simpler `.whatsapp-float:hover` rule added in Plan 05-01.

---

### Task 6: Verify section animations and counter

Run `npm run dev` and verify:

1. Scroll down to the services section — cards should fade-up and spring in one at a time (80ms stagger). If services load instantly visible (no animation), check that `client:visible` hydration fires correctly and the viewport margin is triggering.
2. Scroll to the About section stats row — numbers should count from 0 up to 20, 15, and 2 with a 1.5s ease-out curve.
3. The floating WhatsApp button (bottom-right corner) should have a pulsing green glow ring around it.
4. Enable DevTools > Rendering > "Emulate CSS media feature prefers-reduced-motion: reduce" and reload — counters should jump directly to final values, service cards should appear immediately, no pulse on WhatsApp button.

Run `npm run build` — zero errors expected.

---

## Files Changed

- `src/components/ServicesAnimations.tsx` — new React island: staggered service cards + process steps
- `src/components/ServicesSection.astro` — replaced static grid HTML with `<ServicesAnimations client:visible />`; frontmatter serializes service data
- `src/components/StatsCounter.tsx` — new React island: animated number counters using Motion `useMotionValue`
- `src/components/AboutSection.astro` — replaced static stats display with `<StatsCounter client:visible />`; frontmatter adds parsed stat objects
- `src/layouts/BaseLayout.astro` — added `whatsapp-pulse` `@keyframes` and updated `.whatsapp-float` styles
